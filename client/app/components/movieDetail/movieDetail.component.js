import template from './movieDetail.html';
import controller from './movieDetail.controller';
import './movieDetail.scss';

let movieDetailComponent = {
  bindings: {},
  template,
  controller
};

export default movieDetailComponent;
